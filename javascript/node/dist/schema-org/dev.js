export {};
/*
import { SchemaOrg } from "./schema-org.js";
import { getTypeStatistics } from "./statistics.js";

const schema = new SchemaOrg();

const types = getTypeStatistics("../csv/types.csv");

types.forEach(statistics =>
    {
    console.log(statistics.row);
    });
*/
