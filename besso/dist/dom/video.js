import { getVideoStream } from "./multimedia.js";
//////////////////////////////////////////////////////////////////////////////////////////////////////////////
async function displayVideoStream(element, idealWidth, idealHeight) {
    element.srcObject = await getVideoStream("environment", idealWidth, idealHeight);
}
//////////////////////////////////////////////////////////////////////////////////////////////////////////////
export { displayVideoStream };
