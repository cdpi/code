export {};
/*
import { Type, Property, Enumeration } from "./types.js";
import { Parser } from "./parser.js";

class SchemaOrg
    {
    public constructor()
        {
        }

    public get enumerations():Array<Enumeration>
        {
        return [];
        }

    public get properties():Array<Property>
        {
        return [];
        }

    public get types():Array<Type>
        {
        return [];
        }

    private merge(types:Array<Type>, properties:Array<Property>):void
        {
        }

    private async download(url:string)
        {
        const response = await fetch(url);

        return await response.text();
        }
    }

export
    {
    SchemaOrg
    };
*/
